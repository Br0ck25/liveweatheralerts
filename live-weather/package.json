{
	"name": "live-weather",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build:public": "npm --prefix ../frontend run build && node ./scripts/prepare-public-assets.mjs",
		"deploy": "npm run build:public && wrangler deploy",
		"dev": "npm run build:public && wrangler dev",
		"start": "npm run build:public && wrangler dev",
		"test": "npm run build:public && vitest run",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.5.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.76.0"
	},
	"dependencies": {
		"@block65/webcrypto-web-push": "^1.0.2"
	}
}
