function slugify(text){return String(text||'').toLowerCase().trim().replace(/\s+/g,'-').replace(/[^a-z0-9-]/g,'').replace(/-+/g,'-').replace(/^-+|-+$/g,'');}
function normalizeEventSlugClient(raw){let slug=slugify(raw);if(!slug) return '';slug=slug.replace(/(?<![a-z])advi-?ory(?![a-z])/g,'advisory');slug=slug.replace(/(?<![a-z])warnin?g?(?![a-z])/g,'warning');slug=slug.replace(/floodwatch/g,'flood-watch');slug=slug.replace(/(?<![a-z])high-?surf(?![a-z])/g,'high-surf');slug=slug.replace(/(?<![a-z])windadvi-?ory(?![a-z])/g,'wind-advisory');return slug;}
function getEventSlugVariants(event,eventSlug){const slugs=new Set(); if(!eventSlug) eventSlug=slugify(event||''); if(!eventSlug) return []; slugs.add(eventSlug); if(eventSlug.includes('-')){slugs.add(eventSlug.replace(/-/g,''));}else{slugs.add(eventSlug.replace(/([a-z])([A-Z])/g,'$1-$2').toLowerCase());} if(eventSlug.startsWith('pecial')) slugs.add('s'+eventSlug); if(eventSlug.endsWith('-tatement')) slugs.add(eventSlug.replace(/-tatement$/,'-statement')); if(eventSlug.endsWith('tatement')) slugs.add(eventSlug.replace(/tatement$/,'statement')); const normalizedEvent=String(event||'').toLowerCase(); if(/\bspecial\s+weather\s+statement\b/.test(normalizedEvent)){slugs.add('special-weather-statement'); slugs.add('specialweatherstatement');} return Array.from(slugs);}
console.log(normalizeEventSlugClient('Special Weather Statement')); console.log(getEventSlugVariants('Special Weather Statement', normalizeEventSlugClient('Special Weather Statement')));
